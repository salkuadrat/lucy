import 'dart:async';

FutureOr db(List<String> args) async {
  //
  // Create cli commands for database:
  //
  // l db init
  // l db create
  // l db model --name User --attributes
  // l db migrate
  // l db seed --name users
  // l db seeds
  //
  print('Command db is under development...');
}
